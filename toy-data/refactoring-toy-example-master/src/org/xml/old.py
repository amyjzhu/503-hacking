# with open("index.xml", "r") as f:
    #     content = f.readlines()
    # content = [i.strip() for i in content]
    # print(len(content))

    # start_compounds = []
    # end_compounds = []

    # for i, cont in enumerate(content):
    #     if "/compound" in cont:
    #         end_compounds.append(i)
    #     if "compound" in cont and not i in end_compounds:
    #         start_compounds.append(i)

    # assert len(start_compounds) == len(end_compounds)
    # for s, e in zip(start_compounds, end_compounds):
    #     assert s < e

    # print(start_compounds)
    # print(end_compounds)




        # with open(file, "r") as f:
        #     content = f.readlines()
        # content = [i.strip() for i in content]
        # print(len(content))

        # for j, cont in enumerate(content):
        #     if "compoundname" in cont:
        #         directory_name = cont.

